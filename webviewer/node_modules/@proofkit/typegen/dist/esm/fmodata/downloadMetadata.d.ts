import { z } from 'zod/v4';
import { typegenConfigSingle } from '../types.js';
type FmodataConfig = Extract<z.infer<typeof typegenConfigSingle>, {
    type: "fmodata";
}>;
export declare function downloadTableMetadata({ config, tableName, reduceAnnotations, }: {
    config: FmodataConfig;
    tableName: string;
    reduceAnnotations?: boolean;
}): Promise<string>;
export {};
