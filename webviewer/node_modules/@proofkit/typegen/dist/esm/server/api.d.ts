import { IncomingMessage } from 'node:http';
import { URL } from 'node:url';
export interface ApiContext {
    cwd: string;
    configPath: string;
}
export interface ApiResponse {
    status: number;
    headers: Record<string, string>;
    body: string;
}
export declare function handleApiRequest(req: IncomingMessage, url: URL, context: ApiContext): Promise<ApiResponse>;
