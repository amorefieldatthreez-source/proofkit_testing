import { z } from 'zod/v4';
import { typegenConfig } from './types.js';
type GlobalOptions = Omit<z.infer<typeof typegenConfig>, "config">;
export declare const generateTypedClients: (config: z.infer<typeof typegenConfig>["config"], options?: GlobalOptions & {
    resetOverrides?: boolean;
    cwd?: string;
    configPath?: string;
}) => Promise<{
    successCount: number;
    errorCount: number;
    totalCount: number;
    outputPaths: string[];
} | undefined>;
export {};
