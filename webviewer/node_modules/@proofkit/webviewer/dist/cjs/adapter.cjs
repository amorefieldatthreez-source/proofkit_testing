"use strict";
var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const fmdapi = require("@proofkit/fmdapi");
const main = require("./main.cjs");
class WebViewerAdapter {
  constructor(options) {
    __publicField(this, "scriptName");
    __publicField(this, "request", async (params) => {
      var _a, _b, _c;
      const { action = "read", layout, body } = params;
      if ("_offset" in body) {
        Object.assign(body, { offset: body._offset });
        body._offset = void 0;
      }
      if ("_limit" in body) {
        Object.assign(body, { limit: body._limit });
        body._limit = void 0;
      }
      if ("_sort" in body) {
        Object.assign(body, { sort: body._sort });
        body._sort = void 0;
      }
      const resp = await main.fmFetch(this.scriptName, {
        ...body,
        layouts: layout,
        action,
        version: "vLatest"
      });
      if (((_a = resp.messages) == null ? void 0 : _a[0].code) !== "0") {
        throw new fmdapi.FileMakerError(
          ((_b = resp == null ? void 0 : resp.messages) == null ? void 0 : _b[0].code) ?? "500",
          `Filemaker Data API failed with (${(_c = resp.messages) == null ? void 0 : _c[0].code}): ${JSON.stringify(resp, null, 2)}`
        );
      }
      return resp.response;
    });
    __publicField(this, "list", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        body: data,
        layout
      });
      return resp;
    });
    __publicField(this, "get", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        body: data,
        layout
      });
      return resp;
    });
    __publicField(this, "find", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        body: data,
        layout
      });
      return resp;
    });
    __publicField(this, "create", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        action: "create",
        body: data,
        layout
      });
      return resp;
    });
    __publicField(this, "update", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        action: "update",
        layout,
        body: data
      });
      return resp;
    });
    __publicField(this, "delete", async (opts) => {
      const { data, layout } = opts;
      const resp = await this.request({
        action: "delete",
        body: data,
        layout
      });
      return resp;
    });
    __publicField(this, "layoutMetadata", async (opts) => {
      return await this.request({
        action: "metaData",
        layout: opts.layout,
        body: {}
      });
    });
    __publicField(this, "executeScript", () => {
      throw new Error(
        "the `executeScript` method is not supported in the webviewer adapter. Use the `fmFetch` or `callFMScript` functions from @proofkit/webviewer instead."
      );
    });
    __publicField(this, "containerUpload", () => {
      throw new Error("Container upload is not supported in webviewer");
    });
    this.scriptName = options.scriptName;
  }
}
exports.WebViewerAdapter = WebViewerAdapter;
//# sourceMappingURL=adapter.cjs.map
