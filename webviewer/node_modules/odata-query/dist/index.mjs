const S = ["eq", "ne", "gt", "ge", "lt", "le"], m = ["and", "or", "not"], F = ["any", "all"], I = ["startswith", "endswith", "contains", "matchespattern"], k = [
  "expand",
  "levels",
  "select",
  "skip",
  "top",
  "count",
  "orderby",
  "filter"
], N = /\((.*)\)/, T = /(?!indexof)\((\w+)\)/;
var L = /* @__PURE__ */ ((e) => (e.sum = "sum", e.min = "min", e.max = "max", e.average = "average", e.countdistinct = "countdistinct", e))(L || {});
const G = (e) => ({ type: "raw", value: e }), X = (e) => ({ type: "guid", value: e }), J = (e) => ({ type: "duration", value: e }), x = (e) => ({ type: "binary", value: e }), z = (e) => ({ type: "json", value: e }), H = (e, o) => ({ type: "alias", name: e, value: o }), Q = (e) => ({ type: "decimal", value: e }), j = "";
function W({
  select: e,
  search: o,
  skiptoken: t,
  format: n,
  top: a,
  skip: $,
  filter: s,
  transform: p,
  orderBy: h,
  key: A,
  count: u,
  expand: y,
  action: i,
  func: c
} = {}) {
  let l = "", r = [];
  const f = {};
  if (A != null && (l += `(${O(A, r)})`), (s || typeof u == "object") && (f.$filter = E(typeof u == "object" ? u : s, r)), p && (f.$apply = _(p)), y && (f.$expand = w(y)), h && (f.$orderby = R(h)), u && (typeof u == "boolean" ? f.$count = !0 : l += "/$count"), typeof a == "number" && (f.$top = a), typeof $ == "number" && (f.$skip = $), i && (l += `/${i}`), c) {
    if (typeof c == "string")
      l += `/${c}`;
    else if (typeof c == "object") {
      const [b] = Object.keys(c), d = O(c[b], r);
      l += `/${b}`, d !== "" && (l += `(${d})`);
    }
  }
  return r.length > 0 && Object.assign(f, r.reduce(
    (b, d) => Object.assign(b, { [`@${d.name}`]: O(d.value) }),
    {}
  )), o && (f.$search = encodeURIComponent(o)), V(l, { $select: e, $skiptoken: t, $format: n, ...f });
}
function C(e, o, t = []) {
  return `${e} eq ${O(o, t)}`;
}
function E(e = {}, o = [], t = "") {
  return (Array.isArray(e) ? e : [e]).reduce(($, s) => {
    if (s) {
      const p = n(s, o, t);
      p && $.push(p);
    }
    return $;
  }, []).join(" and ");
  function n($ = {}, s = [], p = "") {
    let h = "";
    return typeof $ == "string" ? h = $ : $ && typeof $ == "object" && (h = Object.keys($).reduce(
      (u, y) => {
        const i = $[y];
        if (i === void 0)
          return u;
        let c = "";
        if (p ? y === j ? c = p : T.test(y) ? c = y.replace(T, (l, r) => r.trim() === j ? `(${p})` : `(${p}/${r.trim()})`) : N.test(y) ? c = y.replace(N, (l, r) => r.trim() === j ? `(${p})` : `(${p}/${r.trim()})`) : c = `${p}/${y}` : c = y, y === j && Array.isArray(i))
          return u.concat(
            i.map((l) => C(c, l))
          );
        if (["number", "string", "boolean"].indexOf(typeof i) !== -1 || i instanceof Date || i === null)
          u.push(C(c, i, s));
        else if (Array.isArray(i)) {
          const l = y, r = i.map((f) => E(f, s, p)).filter((f) => f).map((f) => m.indexOf(l) !== -1 ? `(${f})` : f);
          r.length && (m.indexOf(l) !== -1 ? r.length && (l === "not" ? u.push(g(r)) : u.push(`(${r.join(` ${l} `)})`)) : u.push(r.join(` ${l} `)));
        } else if (m.indexOf(c) !== -1) {
          const l = c, r = Object.keys(i).map(
            (f) => n({ [f]: i[f] })
          );
          r.length && (l === "not" ? u.push(g(r)) : u.push(`(${r.join(` ${l} `)})`));
        } else if (typeof i == "object")
          "type" in i ? u.push(C(c, i, s)) : Object.keys(i).forEach((r) => {
            if (i[r] !== void 0)
              if (S.indexOf(r) !== -1)
                u.push(`${c} ${r} ${O(i[r], s)}`);
              else if (m.indexOf(r) !== -1)
                Array.isArray(i[r]) ? u.push(
                  i[r].map((f) => "(" + n(f, s, c) + ")").join(` ${r} `)
                ) : u.push("(" + n(i[r], s, c) + ")");
              else if (F.indexOf(r) !== -1) {
                const f = a(y.toLowerCase(), i[r], r, c);
                f && u.push(f);
              } else if (r === "has")
                u.push(`${c} ${r} ${O(i[r], s)}`);
              else if (r === "in") {
                const f = Array.isArray(i[r]) ? i[r] : i[r].value.map((b) => ({
                  type: i[r].type,
                  value: b
                }));
                u.push(
                  c + " in (" + f.map((b) => O(b, s)).join(",") + ")"
                );
              } else if (I.indexOf(r) !== -1)
                u.push(`${r}(${c},${O(i[r], s)})`);
              else {
                const f = /\/not/g.test(c);
                let b = f ? c.replace(/\/not/g, "") : c;
                const d = f ? g([n({ [r]: i[r] }, s, b)]) : n({ [r]: i[r] }, s, b);
                d && u.push(d);
              }
          });
        else
          throw new Error(`Unexpected value type: ${i}`);
        return u;
      },
      []
    ).join(" and ")), h;
  }
  function a($, s, p, h) {
    let A = "";
    if (typeof s == "string" || s instanceof String)
      A = U($, s, p, h);
    else if (s) {
      const u = Array.isArray(s) ? s.reduce((i, c) => c.hasOwnProperty(j) ? (i.hasOwnProperty(j) || (i[j] = []), i[j].push(c[j]), i) : { ...i, ...c }, {}) : s, y = n(u, o, $);
      A = `${h}/${p}(${y ? `${$}:${y}` : ""})`;
    }
    return A;
  }
}
function U(e, o, t, n) {
  const a = t == "all" ? "ne" : "eq";
  return `${n}/${t}(${e}: ${e} ${a} '${o}')`;
}
function q(e) {
  return e = e.replace(/'/g, "''"), encodeURIComponent(e);
}
function O(e, o) {
  if (typeof e == "string")
    return `'${q(e)}'`;
  if (e instanceof Date)
    return e.toISOString();
  if (typeof e == "number")
    return e;
  if (Array.isArray(e))
    return `[${e.map((t) => O(t)).join(",")}]`;
  if (e === null)
    return e;
  if (typeof e == "object")
    switch (e.type) {
      case "raw":
      case "guid":
        return e.value;
      case "duration":
        return `duration'${e.value}'`;
      case "binary":
        return `binary'${e.value}'`;
      case "alias":
        return Array.isArray(o) && o.push(e), `@${e.name}`;
      case "json":
        return escape(JSON.stringify(e.value));
      case "decimal":
        return `${e.value}M`;
      default:
        return Object.entries(e).filter(([, t]) => t !== void 0).map(([t, n]) => `${t}=${O(n, o)}`).join(",");
    }
  return e;
}
function w(e) {
  if (typeof e == "number")
    return e;
  if (typeof e == "string")
    return e.indexOf("/") === -1 ? e : e.split("/").reverse().reduce((o, t, n, a) => n === 0 ? `$expand=${t}` : n === a.length - 1 ? `${t}(${o})` : `$expand=${t}(${o})`, "");
  if (Array.isArray(e))
    return `${e.map((o) => w(o)).join(",")}`;
  if (typeof e == "object") {
    const o = Object.keys(e);
    return o.some(
      (t) => k.indexOf(t.toLowerCase()) !== -1
    ) ? o.map((t) => {
      let n;
      switch (t) {
        case "filter":
          n = E(e[t]);
          break;
        case "orderBy":
          n = R(e[t]);
          break;
        case "levels":
        case "count":
        case "skip":
        case "top":
          n = `${e[t]}`;
          break;
        default:
          n = w(e[t]);
      }
      return `$${t.toLowerCase()}=${n}`;
    }).join(";") : o.map((t) => {
      const n = w(e[t]);
      return n ? `${t}(${n})` : t;
    }).join(",");
  }
  return "";
}
function _(e) {
  return (Array.isArray(e) ? e : [e]).reduce((n, a) => {
    for (const $ of Object.keys(a))
      switch ($) {
        // TODO: support as many of the following:
        //   topcount, topsum, toppercent,
        //   bottomsum, bottomcount, bottompercent,
        //   identity, concat, expand, search, compute, isdefined
        case "aggregate": {
          const s = a[$];
          s && n.push(`aggregate(${D(s)})`);
          break;
        }
        case "filter": {
          const s = a[$];
          if (s) {
            const p = E(s);
            p && n.push(`filter(${p})`);
          }
          break;
        }
        case "groupBy": {
          const s = a[$];
          s && n.push(`groupby(${P(s)})`);
          break;
        }
        default:
          throw new Error(`Unsupported transform: ${$}`);
      }
    return n;
  }, []).join("/") || void 0;
}
function D(e) {
  return (Array.isArray(e) ? e : [e]).map((t) => typeof t == "string" ? t : Object.keys(t).map((n) => {
    const a = t[n];
    if (!a.with && a.as)
      return `${n} as ${a.as}`;
    if (!a.with)
      throw new Error(`'with' property required for '${n}'`);
    if (!a.as)
      throw new Error(`'as' property required for '${n}'`);
    return `${n} with ${a.with} as ${a.as}`;
  })).join(",");
}
function P(e) {
  if (!e.properties)
    throw new Error("'properties' property required for groupBy");
  let o = `(${e.properties.join(",")})`;
  return e.transform && (o += `,${_(e.transform)}`), o;
}
function R(e, o = "") {
  return Array.isArray(e) ? e.map(
    (t) => Array.isArray(t) && t.length === 2 && ["asc", "desc"].indexOf(t[1]) !== -1 ? t.join(" ") : t
  ).map((t) => `${o}${String(t)}`).join(",") : typeof e == "object" ? Object.entries(e).map(([t, n]) => R(n, `${t}/`)).map((t) => `${o}${t}`).join(",") : `${o}${String(e)}`;
}
function V(e, o) {
  const t = Object.getOwnPropertyNames(o).filter((n) => o[n] !== void 0 && o[n] !== "").map((n) => `${n}=${o[n]}`);
  return t.length ? `${e}?${t.join("&")}` : e;
}
function g(e) {
  return `not (${e.join(" and ")})`;
}
export {
  j as ITEM_ROOT,
  L as StandardAggregateMethods,
  H as alias,
  x as binary,
  Q as decimal,
  W as default,
  J as duration,
  X as guid,
  z as json,
  G as raw
};
